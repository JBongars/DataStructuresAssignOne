#ifndef DS_STRINGSTATIC_H
#define DS_STRINGSTATIC_H

#include <string>
#include <vector>
#include <iostream>

namespace DS_StringStatic {
    std::vector<std::string> split(std::string input, std::string delimeter);
    int convertToInt(std::string input);
};

#endif // DS_STRINGSTATIC_H
